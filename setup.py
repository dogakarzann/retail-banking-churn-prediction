from setuptools import setup, find_packages

setup(
    name="retail_banking_churn",
    version="0.1.0",
    description="Retail Banking Churn Prediction utils and modules.",
    packages=find_packages(),
)
